{
  "name": "jmying",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "audit:security": "node scripts/security-audit.mjs",
    "test": "node --check public/app.js && node scripts/security-audit.mjs && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.68.0"
  }
}
