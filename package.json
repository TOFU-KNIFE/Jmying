{
  "name": "jmying",
  "version": "1.15.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "format": "prettier --write . --ignore-path .gitignore",
    "format:check": "prettier --check . --ignore-path .gitignore",
    "audit:i18n": "node scripts/i18n-audit.mjs",
    "audit:performance": "npm run build --silent && node scripts/performance-audit.mjs",
    "audit:quality": "node scripts/quality-audit.mjs",
    "audit:security": "node scripts/security-audit.mjs",
    "check": "node --check public/app.js && node --check scripts/build.mjs && node --check scripts/i18n-audit.mjs && node --check scripts/quality-audit.mjs && node --check scripts/performance-audit.mjs && node --check scripts/security-audit.mjs",
    "test": "npm run check && npm run audit:i18n && npm run audit:quality && npm run audit:performance && npm run audit:security && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@fontsource-variable/inter": "5.3.0",
    "@fontsource-variable/source-serif-4": "5.3.0",
    "clean-css": "5.3.3",
    "html-minifier-terser": "7.2.0",
    "prettier": "3.9.6",
    "terser": "5.49.0",
    "wrangler": "4.115.0"
  }
}
